<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, :placeholder => "Type your name here"  %>
  </div>
  <div class="form-group">
    <%= f.label :email %><br>
    <%= f.text_field :email, :placeholder => "Type your email here."  %>
  </div>
  <div class="form-group">
    <%= f.label :message %><br>
    <%= f.text_area :message, size: "24x6", :placeholder => "Type your message here."  %>
  </div>

  <div class="form-group">

   <%= f.submit :value => "Send Email", :class => 'btn brn-lg btn-primary' %>

  <!--<% if  current_page?(root_path) %>
    <p><%= link_to 'Send Email', new_user_path, class: "btn brn-lg btn-primary" %></p>
 <% else %>
    <%= f.submit :value => "Send Email", :class => 'btn brn-lg btn-primary' %>
 <% end %>
-->
   <!--<%= f.submit :value => "Create New Email Subscription User", :class => 'btn brn-lg btn-primary' %> -->

  
  </div>
<% end %>
